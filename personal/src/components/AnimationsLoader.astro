<style is:global>
  .comeFromBelow {
    transform: translateY(-1000);
    opacity: 0;
  }
</style>

<script>
  import anime from "animejs";

  var tl = anime.timeline({
    easing: "easeInOutSine",
    duration: 250,
  });

  tl.add({
    targets: ".comeFromBelow",
    translateY: [70, 0],
    opacity: [0, 1],
    delay: anime.stagger(50),
  });

  tl.add({
    targets: "#techstackify li",
    translateY: [70, 0],
    opacity: [0, 1],
    delay: anime.stagger(5),
  });

  // Find our component DOM on the page.
  // const links = document.querySelectorAll("[data-transition-page-link]");

  // // Add event listeners to fire confetti when a button is clicked.
  // links.forEach((l) => {
  //   l.addEventListener("click", (event) => {
  //     // event.preventDefault();
  //     // @ts-ignore
  //     // event.currentTarget.classList.add("invisible");
  //     anime({
  //       targets: ".comeFromBelow",
  //       translateY: [0, 70],
  //       opacity: [1, 0],
  //       duraton: 2,
  //       delay: anime.stagger(5),
  //       complete: (anim) => {
  //         // @ts-ignore
  //         // event.currentTarget.classList.add("invisible");
  //       },
  //     });
  //   });
  // });
</script>
